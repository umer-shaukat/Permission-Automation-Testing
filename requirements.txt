selenium==4.11.2         # Latest stable version as of now (update if newer)
pytest==7.4.2            # Latest stable version as of now
pytest-html==3.2.0       # Plugin for generating HTML reports in pytest
